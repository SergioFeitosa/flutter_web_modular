import 'package:flutter/material.dart';

class PessoaPage extends StatelessWidget {

  const PessoaPage({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Pessoa Page'),),
           body: Container(),
       );
  }
}